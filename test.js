const a = 5,
      b = 10,
      c = 15,
      d = 20;

      function calcul (x,y){
          const z = x - y
          return z
      }

      calcul (d,b)
      calcul (c,a)
      calcul (d,c)

      console.log(calcul(d,c))